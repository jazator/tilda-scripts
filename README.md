# Tilda scripts (Скрипты для Тильды)

Custom JavaScript scripts for Tilda (T123).

Этот репозиторий — набор практичных скриптов для Тильды, которые можно быстро скопировать и вставить на страницу, чаще всего через блок **T123 (HTML)**.

---

## Как пользоваться

1) Открой нужный файл со скриптом в папке `scripts/`  
2) Скопируй код и вставь в Тильде (обычно в блок **T123**)  
3) Опубликуй страницу и проверь в браузере

---

## Каталог скриптов

1. **SV102 clickable** — делает карточку/блок целиком кликабельным (не только кнопку) → файл: [`scripts/sv102-clickable.js`](scripts/sv102-clickable.js)

<details>
  <summary><b>SV102 clickable — подробности</b></summary>

  **Что делает**  
  Делает весь элемент карточки кликабельным: клик по карточке ведёт по ссылке внутри неё.
  
  **Если не сработало**  
  Скорее всего, на твоей странице отличаются классы/структура блока. Тогда нужно заменить селекторы `.t847__item` и/или `a.t-card__link` на актуальные.

</details>

2. **A/B test (2 blocks)** — случайно показывает блок A или B и запоминает выбор (localStorage) → файл: [`scripts/ab-test-2-blocks.js`](scripts/ab-test-2-blocks.js)

<details>
  <summary><b>A/B test (2 blocks) — подробности</b></summary>

  **Что делает**  
  Случайно показывает один из двух блоков (A или B) и сохраняет выбор в `localStorage`.  
  При повторном визите пользователь увидит тот же вариант.

  **Как настроить**  
  1) Найди ID своих блоков Тильды.  
  2) В файле скрипта замени значения:
  - `A: 'rec...'`
  - `B: 'rec...'`

</details>

3. **BF204N калькулятор: разделители тысяч** — форматирует итог калькулятора: `120000 → 120 000` → файл: [`scripts/bf204n-calc-thousands.js`](scripts/bf204n-calc-thousands.js)

<details>
  <summary><b>BF204N калькулятор: разделители тысяч — подробности</b></summary>

  **Что делает**  
  Форматирует число в элементе результата калькулятора (класс `.t-calc`): добавляет пробелы как разделители тысяч (`120000 → 120 000`).  
  Поддерживает знак (`-120000`) и дробную часть (`12345,67` / `12345.67`).

  **Как настроить**  
  По умолчанию скрипт работает по всему сайту (`ROOT_SELECTOR = 'body'`).  
  Если на сайте несколько калькуляторов — рекомендую ограничить область:  
  1) Найди ID нужного блока в Тильде (например `rec1803879921`)  
  2) Включи строку и подставь свой ID:
  - `var ROOT_SELECTOR = '#rec1803879921';`

</details>

4. **Non-RU only blocks (browser language)** — показывает заданные блоки только если язык браузера НЕ `ru` → файл: [`scripts/non-ru-only-blocks.js`](scripts/non-ru-only-blocks.js)

<details>
  <summary><b>Non-RU only blocks — подробности</b></summary>

  **Что делает**  
  Показывает указанные блоки только для пользователей, у которых язык браузера НЕ начинается с `ru` (например, `en-US`, `de-DE`).  
  Если язык `ru` / `ru-RU` — блоки скрываются.

  **Как настроить**  
  В файле скрипта заполни массив `NON_RU_BLOCKS` своими ID блоков Тильды вида `rec123456789`.

</details>

5. **QZ101 selection limit (2 options)** — ограничивает множественный выбор в квизе: можно отметить не больше 2 вариантов → файл: [`scripts/QZ101-selection-2.js`](scripts/QZ101-selection-2.js)

<details>
  <summary><b>QZ101 selection limit (2 options) — подробности</b></summary>

  **Что делает**  
  В квизе/форме Тильды с множественным выбором (checkbox) ограничивает выбор: максимум **2** варианта в группе.  
  Если пользователь пытается выбрать третий — галочка снимается и появляется подсказка возле клика.

  **Как настроить**  
  В файле скрипта поменяй:
  - `LIMIT = 2` — лимит выбора (если нужно другое число)
  - `FORM_ID = 'form...'` — ID твоей формы/квиза 
  - `MESSAGE_TEXT` — текст подсказки

  **Если не сработало**  
  Чаще всего причина — неверный `FORM_ID` (форма другая) или у квиза другая структура. Тогда нужно уточнить селекторы.

</details>

6. **TE225 show more cards** — скрывает карточки и показывает по кнопке BF101 заданным количеством → файл: [`scripts/te225-show-more-cards.js`](scripts/te225-show-more-cards.js)

<details>
  <summary><b>TE225 show more cards — подробности</b></summary>

  **Что делает**  
  Скрывает карточки TE225 при загрузке и показывает их по кнопке BF101 заданным количеством.

  **Как настроить**  
  В файле скрипта поменяй:
  - `INITIAL_VISIBLE` — сколько карточек видно сразу
  - `PER_CLICK` — сколько карточек показывать за один клик
  - `CONTAINER_SELECTOR` / `CARD_SELECTOR` / `BUTTON_SELECTOR` — селекторы (если структура отличается)

</details>

---

## Дисклеймер

Код предоставляется **как есть**. Не гарантируется, что скрипты будут работать бесконечно долго: Тильда иногда меняет блоки и внутренний код формирования страниц.

Если скрипт перестал работать из-за изменений на стороне Тильды — исправление таких ошибок **от 500 ₽**.

---

## Внедрение и разработка

Могу:
- внедрить скрипты на твой сайт Тильды,
- написать новый скрипт под задачу,
- настроить интеграции Тильды с базами данных и ботами для администрирования.

Обращайся даже с самыми смелыми идеями 🙂

**Связь:** Telegram ['@exkur_altai'](https://t.me/exkur_altai)

---

## Лицензия

MIT License (см. файл `LICENSE`).
